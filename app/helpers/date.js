

/* Can't get ember-cli to recognize momentJS yet so helper isn't working
   skip for now and fix later


import Ember from "ember";

export default Ember.Handlebars.registerBoundHelper('date', function(date) {
  return moment(date).format('MMM Do');
});

*/